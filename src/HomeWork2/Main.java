package HomeWork;

import java.beans.Statement;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
/*
public class Main {
    static final String url = "jdbc:postgresql://localhost:5432/";
    static final String user = "postgres";
    static final String password = "postgres";

    /*public static void main(String[] args) {
        Products p = createNewProduct("Milk");
        
        Products saved = insertProduct(p);
    }
    private static Products insertProduct(Products p) {
        try {
          /*  String sql = "insert into products volues (" + p.getName + ")";
            Connection connection = DriverManager.getConnection(url, user, password);
            Statement statement = (Statement) connection.createStatement();
            ResultSet resultSet = statement.executeQuery(sql);
            resultSet.getInt("id");
            int id = 0;
            p.setId(id);
        } catch (SQLException throwables) {
            throwables.printStackTrace();
        }
        return p;
    }
    private static Products createNewProduct(String name) {
        return new Products(name);
    }
    }
*/